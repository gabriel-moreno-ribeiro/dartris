name: dartris
description: Tetris written from scratch in Dart, playable in the terminal.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dev_dependencies:
  test: ^1.24.0
  lints: ^2.1.0
